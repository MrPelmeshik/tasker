# ===========================================
# Tasker App — переменные окружения (пример)
# ===========================================
# Скопируйте в .env и задайте значения. В production обязательно задать REACT_APP_API_BASE.

# Базовый URL API (в production обязательно задать)
REACT_APP_API_BASE=http://localhost:8080

# Базовый путь приложения (по умолчанию /tasker)
REACT_APP_BASE_PATH=/tasker

# Путь SignalR Hub (по умолчанию /hubs/tasker)
REACT_APP_HUB_PATH=/hubs/tasker

# Буфер в минутах для проактивного обновления access-токена (2–15, по умолчанию 5)
REACT_APP_TOKEN_REFRESH_BUFFER_MINUTES=5
