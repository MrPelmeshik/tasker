/**
 * Брейкпоинты для JS (например, window.innerWidth).
 * Синхронизировано с --bp-* в index.css.
 */

export const BREAKPOINTS = {
  sm: 640,
  md: 768,
  lg: 900,
  xl: 1000,
  '2xl': 1280,
  '3xl': 1600,
} as const;
